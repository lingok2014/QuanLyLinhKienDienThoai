@model IEnumerable<QuanLyLinhKienDienThoai.Models.THUONGHIEU>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/Layout/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TENTH)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HINHTH)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TENTH)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HINHTH)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MATHUONGHIEU }) |
            @Html.ActionLink("Details", "Details", new { id=item.MATHUONGHIEU }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MATHUONGHIEU })
        </td>
    </tr>
}

</table>
